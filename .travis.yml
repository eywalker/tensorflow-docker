sudo: required

language: generic

services:
  - docker

script:
  - docker build -t eywalker/tensorflow:cuda .

after_success:
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker push eywalker/tensorflow:cuda
